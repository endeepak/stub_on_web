<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :path, "Path", class: "control-label" %>
    <%= text_input f, :path, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :response_status, "Response status", class: "control-label" %>
    <%= number_input f, :response_status, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :response_body, "Response body", class: "control-label" %>
    <%= textarea f, :response_body, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
